import { MiniRowDirective } from './mini-row.directive';

describe('MiniRowDirective', () => {
  it('should create an instance', () => {
    const directive = new MiniRowDirective();
    expect(directive).toBeTruthy();
  });
});
