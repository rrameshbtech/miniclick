import { MiniColumnDirective } from './mini-column.directive';

describe('MiniColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new MiniColumnDirective();
    expect(directive).toBeTruthy();
  });
});
