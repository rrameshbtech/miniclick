import { MiniHeaderRowDirective } from './mini-header-row.directive';

describe('MiniHeaderRowDirective', () => {
  it('should create an instance', () => {
    const directive = new MiniHeaderRowDirective();
    expect(directive).toBeTruthy();
  });
});
